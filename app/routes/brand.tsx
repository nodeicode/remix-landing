import { BrandPage } from "~/components/brand/BrandPage";

/**
 * /brand — the new personal-brand page ("PROTOCOL LO").
 * To make this the homepage, point the index route here
 * (see app/routes/index.tsx), or copy this route's render.
 */
export default function Brand() {
	return <BrandPage />;
}
