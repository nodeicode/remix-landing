import { BrandPage } from "~/components/brand/BrandPage";

/**
 * HOMEPAGE — clean personal-brand page.
 * Editorial hero + Experience (from real myWork content) + Skills + About + Contact.
 * Rollback: /brand (v1), /legacy-landing (original).
 */
export default function Index() {
	return <BrandPage />;
}
